# Gradient Boosting with fast learning
model_class: GradientBoostingClassifier
description: "Gradient Boosting with fast learning (lr=0.1, n_est=100)"

# From base/ensemble_models.yaml
random_state: ${random_state}
n_estimators: 100

# Model-specific
learning_rate: 0.1
max_depth: 3
