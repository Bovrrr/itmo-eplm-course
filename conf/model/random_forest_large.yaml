# Random Forest with 200 trees and unlimited depth
model_class: RandomForestClassifier
description: "Random Forest with 200 trees and unlimited depth"

# From base/tree_models.yaml
random_state: ${random_state}
n_jobs: -1
min_samples_split: 2
min_samples_leaf: 1

# Model-specific
n_estimators: 200
max_depth: null
